// Implemented by docs/features/dark-mode.md (toggle click handling + theme
// cookie persistence). Loaded on every page per docs/features/home.md, but
// intentionally empty until that feature is built.
